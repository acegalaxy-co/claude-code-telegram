---
name: npm publisher
description: Who can publish to @acegalaxy/claude-code-telegram on npm.
type: reference
---

Active publisher account: `kanelr` (verified via `npm whoami`).

If `npm publish` fails with 403, the user running the release is not authenticated. Run `npm login` first.

Org `@acegalaxy` may have additional publishers — check via `npm owner ls @acegalaxy/claude-code-telegram` if needed.
